name = "mesh"
version = "@PACKAGE_VERSION@"
description = "Interface to various mesh generators"
requires = "bigarray"
archive(byte) = "mesh.cma mesh_triangle.cma"
archive(native) = "mesh.cmxa mesh_triangle.cmxa"

package "display" (
  description = "Display meshes using the Graphics module"
  version = "@PACKAGE_VERSION@"
  requires = "mesh graphics"
  archive(byte) = "mesh_display.cma"
  archive(native) = "mesh_display.cmxa"
)
